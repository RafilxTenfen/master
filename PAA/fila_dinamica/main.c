#include <stdio.h>
// #include <stdlib.h>
#include "Fila_publica.h"


int main(int argc, char** argv) {

  // int result;
  // pFila f = criar(sizeof(int), &result);
  // printf("Criar result: %d", result);

  // int enf1 = 4;
  // enfileirar(f, &enf1, &result);
  // printf("\nenfileirar result: %d", result);

  // int enf2 = 9;
  // enfileirar(f, &enf2, &result);
  // printf("\nenfileirar result: %d", result);

  // int enf3 = 15;
  // enfileirar(f, &enf3, &result);
  // printf("\nenfileirar result: %d", result);

  // int *desenf;
  // desenf = desenfileirar(f, &result);
  // printf("\ndesenfileirar result: %d -> %d", result, *desenf);

}
